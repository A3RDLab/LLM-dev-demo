# 课堂实录与教学要点

> 本文整理自 2026 年 8 月 8 日"通号 智能体开发实操演示"培训的录音转写，
> 覆盖[实验手册.md](实验手册.md)实验 0~11 的课堂讲解，外加两个手册之外的专题。
> 录音转写存在识别错误，已甄别修正，常见错误对照见文末附录一。

**阅读方式**：命令与操作步骤以实验手册为准（均已实机验证），本文不重复罗列；
本文承载的是课堂上讲的"为什么"、真实案例与现场实录——适合作为手册的配套讲义或复盘材料。

---

## 通用学习法（课堂开场要点）

1. **AI 辅助下，命令不必死记**。所有实验都在 AI 辅助条件下进行，可以让 IDE 里的编码助手参考实验手册自己完成环境准备与命令执行，学习过程可以更丝滑。
2. **用任何你熟悉的 IDE 打开仓库即可**。讲师课堂用的是 Qoder（它有 IDE 风格与对话风格两种形态，可以结合使用），用 VS Code 等也类似。
3. **分清 IDE 的几种模式**：ask 模式只回答问题、不改动任何本地文件；agent 模式会实际修改文件；plan 模式会先写出修改计划再执行（详见专题 A）。
4. **让 AI 帮你配环境**：把拿到的 `.env` 内容直接粘贴到编码助手侧边栏，说一句"帮我用以下设置配置好实验环境"即可。用 `.env` 导入环境变量做运行配置是标准做法。
5. **实验用模型不需要很强**。讲师的编码助手用的是 Qwen3.8-Max，但手册里的全部示例都在 qwen3.6-27b/35b 这类百亿参数级中档模型上跑通过（课堂发放的百炼平台 Key 白名单即这两个对话模型 + 一个 `qwen3.7-text-embedding` 向量模型）。
6. **看不懂的代码让 AI 讲解**。本仓库每个 demo 都是自包含脚本，随时可以问"这段代码在做什么"。

---

## 实验 0~11 逐节教学要点

### 实验 0：环境准备

**讲解要点**

- 为什么每个项目要建自己的虚拟环境：Python 的包管理决定了不同项目、不同版本之间各有自己的依赖要求，面向不同类型的项目创建独立虚拟环境很有必要。
- **OpenAI 兼容 API 已是事实标准**：现在基本上所有大模型厂商都提供 OpenAI 兼容端点；不止模型厂商，一些智能体产品（如 Hermes Agent）也支持接入任意 OpenAI 兼容 API。所以本仓库整体用 OpenAI 兼容接口访问，一次学会处处可用。
- 起步动作：`git clone` 把仓库拉到本地（讲师的仓库已在本地，新环境从 clone 开始）。
- 遇到模型配置/使用问题时，对照手册 0.4 的错误对照表排查（401 查端点、403 查模型白名单、404 查模型名）。

### 实验 1：最简单的单次调用

**讲解要点**

- 大模型虽然表现出各种各样的能力，但**基本功能只有一个：token 输入、token 输出**。大家用的最多的接口是 chat completion：输入一段对话式文本，输出一段文本。
- 对话式文本遵循 `role + content`（谁说了什么话）的格式，角色主要分三类：`user`（用户说的）、`assistant`（AI 回的）、`system`（背景/系统提示词）。
- `temperature` 参数一句话带过：越接近 0 输出确定性越好，越高随机性越强；现在很多模型 API 背后有优化过的默认值，不传也可以。其余参数感兴趣可以让 AI 讲。

**课堂案例：系统提示词的价值——通信"网格"**

一家通信基础设施公司的行业里有个概念叫"网格"，指埋地通信管线的剖面网格（管道按格子租给运营商）。AI 起初总把它误解成"网格计算（grid）"。解法：把业务场景写进系统提示词（"本场景为通信地下管线建设，网格指管线剖面网格"），之后再谈"网格"，AI 瞬间就懂了。

**课堂实录："博士生提问"对照**

本实验脚本的系统提示词是"你在回答博士生的提问。"。同样一句"我想咨询一下关于深度求索的问题"，普通问答场景模型可能回答得很简略，而在这条提示词下，模型给出了模型架构与训练细节、开源资源、复现路径、学术对比等分节深入的回答——回答风格与深度确实被系统提示词引导了。

> 行业应用启示：客户常说"我们行业有些知识大模型不知道"，可以先尝试在系统提示词里补上行业语境再判断。不用编程的低代码平台（Coze、Dify 的工作流节点）同样有系统提示词的位置，做法一致。

### 实验 2：流式多轮对话与推理模型

**课堂案例：黑猫起名**

讲师先说"我是一名程序员"，聊了几轮后说"我养了一只小黑猫，给它起名字有什么建议"。模型记住了前文的程序员身份，起名建议特意贴合程序员文化，还来了一句"黑猫在程序员圈子里特别受欢迎——和终端的背景色很契合"（讲师的终端正好也是深色配色）。这就是多轮记忆的直接体现。

**讲解要点**

- **多轮对话的本质**：LLM 无状态，只有一个"往后生成 token"的功能。实现多轮就是用一个循环，把每轮用户输入与模型输出不断 `append` 进 `messages` 数组，再整体传回去生成——所谓"记忆"就是这么来的。
- 本脚本 `temperature=0.7`；很多模型 API 有优化过的默认值，不显式传也行。
- 流式输出：`stream=True` 后逐 chunk 打印增量内容（一个 chunk 可能是一个或多个 token）；同时兼容有/无深度思考的模型——思考过程灰色打印，最终答案用终端默认色打印（全仓库统一着色约定）。

### 实验 3：结构化输出

**讲解要点（动机先行）**

- 为什么需要结构化输出：大模型默认输出的是成段文字，但对接数据库读写等下游系统时必须按字段输出，段落文字没法直接用；
- 另一个好处是**省 token**：只需输出必要字段，不必长篇累牍。

**关键机制**

- 调用 `chat.completions` 时多了一个 `response_format`（`json_schema` 模式）——这就是指定输出格式的关键；
- **JSON Schema** 规定了输出 JSON 应有哪些字段、每个字段什么类型；本脚本用 Pydantic 定义数据模型，再由 `model_json_schema()` 自动生成 Schema 传入调用——模型定义即契约；
- Pydantic 本身与大模型无关，只是负责把返回的 JSON 解析/校验成 Python 对象；
- 运行效果：模型严格按 Schema 输出 JSON，每个字段都可以直接映射到数据库字段，精准对接后续的结构化数据处理。

### 实验 4：Function Calling——Agent 的最小内核

**讲解要点（第一性原理）**

- 智能体有一个关键点：**通过工具调用外部系统**。接入方式就是封装一个函数，函数里调外部 API，其他代码调这个函数——这就是 function call 的朴素含义。
- 但大模型只能输入/输出 token，自己调不了外部系统。解法：先告诉模型本次任务可用哪些 function；模型输出一段"调用表达"（调哪个函数、传什么参数）；**另一段代码看到输出后替它执行，再把结果返回给模型**。
- ERP 类比：系统接入了企业 ERP，你能做的是告诉模型 ERP 有哪些接口；模型不能直接访问 ERP，它输出"现在访问 ERP 的 A 接口，参数是 X"，智能体代码按这个表达去真正调用——这就是 function calling 的全部。

**课堂实录：ChatALL 代码分析全过程**

默认任务是分析开源项目 [ai-shifu/ChatALL](https://github.com/ai-shifu/ChatALL)（可同时对接多个大模型并综合回答）"是如何接入 OpenAI 的"：

1. 第一次工具调用：`get_repo_tree` 获取仓库目录树（输出做了截断）；
2. 结合目录树挑选文件，多轮 `get_repo_file_content` 读取代码；
3. 模型判断信息足够后给出结论：ChatALL 通过**两种方式**接入 OpenAI——官方 API 与 OAuth。

把循环串起来看：用户问题 → 模型思考并规划下一步（直接回答 / 调工具 / 求助）→ 执行工具（函数就是调一下数据源接口再返回，毫秒级）→ 把"用户问题 + 思考 + 工具调用 + 工具结果"全部拼回 `messages` 再输入模型 → 循环直到给出最终答案。它同时用到了前面两个实验的能力：**多轮拼装**承载历史，**结构化输出**保证模型能精准表达"调哪个函数、传什么参数"。

**标准化的来历**

- `tools` 这套输入格式最初由 OpenAI 提出，后来被各厂商继承——因为它在训练时就是按这个格式做的对齐训练，模型对这种输入的理解和输出都很准确，遂成事实标准；
- 本质上 tools 描述最终会拼进 prompt 的一部分，所以 function calling 归根结底是提示词工程。

**延伸案例：DeepSeek-R1 为什么也能做工具调用**

R1 官方并未支持工具调用（当年要做智能体只能用 V3）。但一些云服务商部署的 R1 却能做智能体——原因是服务商在接口层自动注入 few-shot 提示：给模型示范"收到什么样的输入、应输出什么样的标准工具调用格式"，输出就能被 SDK 解析执行了。代价是没有针对性训练、准确率不如原生支持。这个案例说明：**工具调用 = 上下文 + 约定格式的输出 + 外部代码代执行**，没有任何魔法。

### 实验 5：LangGraph——把手写循环变成"图"

**讲解要点**

- LangGraph 可以粗略理解为**代码版的 Coze/Dify 工作流**：在低代码平台画布上加节点、连线，在 LangGraph 里就是 `add_node` / `add_edge` 写代码（严格意义上并不完全等同，但思路非常类似）；
- 一个显著区别：低代码工作流里的"智能体节点"内部是一个黑盒（提示词+工具打包在一起），而 LangGraph 里**模型节点与工具调用节点是分离的**——什么时候用了工具、用了几次，在轨迹里看得清清楚楚；
- 手写版近 300 行的逻辑（agent 循环、工具定义与解析、流式/思考兼容），在 LangGraph + LangChain 封装下缩到一百多行；`@tool` 一行装饰器就等价于手写版里一大段 JSON 工具定义——**格式标准化了，就该由库来封装，不必人人重写**。

**教学主张：什么该做成工具**

课堂用 `celsius_to_fahrenheit`（摄氏度转华氏度）这个"小工具"说明一个现实项目原则：**公式计算、行业小模型、数据查询逻辑、规则判断，都应该封装成函数做成工具给模型调**，把公式计算与模型判断结合起来；不提倡把复杂的数据处理和计算丢给模型靠深度思考硬算——尽管模型数学能力因 GRPO 等强化学习训练大幅提升，硬算仍然准确率低、速度慢、token 消耗大。而函数计算是毫秒甚至微秒级、结果确定。

**课堂实录**

默认问题"北京和上海的天气怎么样？如果北京更热，帮我算一下北京气温的华氏度是多少？"触发两次工具调用：先 `get_weather` 拿到两地气温（mock 数据），比较出北京更热，再调 `celsius_to_fahrenheit` 换算——条件判断归模型，数值计算归工具，正是上面的主张。

### 实验 6：MCP 协议——工具即服务

**讲解要点（动机算术）**

- 框架（LangChain/LangGraph）封装了智能体逻辑，但**不管外部系统对接代码**——工具函数还是得每个智能体自己写；
- 算一笔账：20 个智能体 × 20 个外部系统 = 全系统散落 400 段对接函数代码，管理维护是灾难；
- MCP 的解法：把对接代码放进 **MCP server**，通过标准协议暴露给任意 **MCP client**（智能体）。注意 MCP **并没有省掉**对接代码——该写的还得写——但它把代码**挪了位置**：只在 server 写一遍，20 个还是 200 个智能体都直接接入；外部系统接口更新时只需同步维护 server 一处。
- client 端不再本地定义工具，而是 `load_mcp_tools` 从 server **运行时发现**；加载时先拿到的只是每个工具的一句话描述——MCP 在某种程度上也体现了渐进式披露的思想。
- server 与 client 之间走 MCP 协议，底层基于流式传输的 HTTP（Streamable HTTP）远程通信。

**课堂实录（翻车与修复）**

现场启动 client 时报了 `Unexpected keyword` 参数错误。排查结论：两段代码依赖的 mcp SDK 库版本不同，参数名不一致导致不兼容；做了兼容性处理后恢复正常。这正印证手册里的版本钉住策略（`mcp[cli]>=1.9,<2`）——依赖版本不一致是真实的事故来源。

本 demo 的 server 封装了三个工具：`get_weather`（查天气）、`get_current_time`（查当前时间）、`lottery`（抽幸运数字，演示无参数工具）。客户端提问"天气怎么样，顺便告诉我几点了"后，模型依次远程调用相应工具作答——对模型而言，远程工具与本地工具的使用方式完全一致。

### 实验 7：RAG——检索增强生成

**课堂实录：一次"拒答"现场教学**

讲师先只敲了"ECS"三个字回车，模型检索不到严格对应的文档，**如实拒答**（"根据文档没有合适答案提供"）；改问"ECS 无法连接怎么查"，立刻命中相关文档并给出排查步骤。两个教训：

1. **跟 AI 说话一定要说清楚**——问题太泛，检索无从命中；
2. **RAG 是很好的幻觉抑制手段**——模型不瞎说，一定遵循你的文档回答（检索分数即向量余弦相似度，0~1 之间）。

**讲解要点**

- 管线：文档 → 格式转换与清洗（去掉 HTML 标签）→ 切块 → embedding 向量化入库 → 按 query 向量检索 → 结合参考资料与问题生成回答；
- 本 demo 的索引在内存/本地文件缓存，没有真写向量数据库，但写法与真实向量库类似；
- query 向量化很快，检索后做归一化处理，最终按"参考资料 + 用户问题"的约束生成。

### 实验 8：Wiki 导航式知识库 Agent

**课堂实录：工行海外布局对比**

课堂提问："工商银行最近两年的海外布局有什么变化？"导航轨迹：

1. 先读顶层 `index.md`，发现 2024、2025 两份年报知识库；
2. 分别进入两个年报各自的 `index.md`（**目录里还有下一层 index，逐级下钻**）；
3. 从目录中锁定"国际化经营"相关页面，读取内容；
4. 综合两年材料给出对比结论：海外布局略有变化，变化不大。

重点不是答案，而是轨迹本身：从一个个 index 下去、找到目录、定位文件、解读内容——知识库的组织结构就是检索结构，全程可解释。讲师补充：这套知识库正是前一天课堂上用对话方式从 PDF 构建出来的（构建方法见专题 B）。

### 实验 9：Text2SQL——自然语言查数据库

**讲解要点**

- 演示库 Chinook 是一个音乐商店示例数据库，非常适合 Text2SQL 教学；
- 默认复合问题（哪个国家客户总消费最高 + 消费前 3 的客户）会被拆成多轮查询——答案"美国"。

**课堂实录：追问探索性数据分析（EDA）**

讲师现场追加了一个开放任务："对这个数据库做探索性数据分析"。模型自行脑爆分析视角——最大的播放列表、最高与最低的收入、收入是否稳定、热门艺术家与音乐、客户分布等——逐一写 SQL 验证后汇总结论。教学点：没有 AI 时 EDA 很费劲（要自己想"该看什么"再逐个试）；有了 AI，一句"做探索性数据分析"，它自己转化问题、写查询、总结发现。它会揣摩"这个库的主人最关心什么"，从有用的角度组织结论。

### 实验 10：Excel 数据分析应用

**讲解要点（动机链）**

1. SQL 查得出数据，但**更复杂的分析要写 Python 代码执行**，SQL 不够；
2. 实际工作中大量数据在 Excel 里——几乎所有业务系统都提供"导出 Excel"功能，因为很多数据分析任务本来就要导出来做；
3. 还常需要**跨多个系统**把数据拉通在一起分析；
4. 互联网、快消等行业做个性化运营，基于 Excel 的分析非常普遍。所以"AI 处理 Excel"是个既典型又长期存在的场景。

**课堂实录**

- 上传《2023年8月-9月销售记录.xlsx》后，讲师**故意换了个题目**（不用手册里预置的问题，避免"背题"嫌疑）："找出销量最高的五个产品，并和销售额最高的五个产品做对比"；
- 首次运行报错提示需要 Docker——**报错即教学点**：AI 生成的代码不是直接在本地跑的，而是在**沙箱（隔离容器）里运行**，这是让大模型写代码的场景必须注意的安全设计；
- 轨迹：把 Excel 拷进沙箱 → agent 写分析代码 → 代码作为字符串传给"沙箱执行代码"的工具 → 拿结果 → 不够就继续写下一段代码；产物（如图表）再从沙箱拷出来；
- 分析结论与常识相符：**销量 Top5 是低价高性价比产品，销售额 Top5 是高价旗舰**——量大价低换来的是市场份额与用户基数，不是高利润。

**读码指引**

这个应用代码看起来复杂，但多出来的绝大部分是 Streamlit 界面与进度展示（前端代码），不必纠结；核心仍是前面实验 4 见过的那个 agent 循环：`messages` + 工具 JSON 串输入模型，循环推进。读不懂的局部随时让 AI 讲解。

### 实验 11：Agent Harness 与 Skills

**讲解要点（演进总结线）**

整门课的递进在此收口：基础接口调用 → 多轮对话（messages 拼装）→ 结构化输出 → function calling（模型能精准表达"调哪个函数、传什么参数"，外部代码代执行，第一个智能体循环诞生——2023 年 OpenAI 发布该能力时令人震撼，但拆开看原理就是这么朴素）→ LangGraph/MCP 等标准化与框架化 → Text2SQL/Excel 数据分析 → Skill。由此，模型不再是框在黑盒里的模型，而是能感知外部数据、推动外部系统的智能体。

**课堂实录**

- 默认任务内置在脚本里：分析 `data/` 中的销售 Excel，按 xlsx skill 规范生成分析报告——不用再手写"拷文件进沙箱、生成代码、执行、展示界面"那一整套，**通过 Skill 直接获得 Claude Code 官方出品的专业 Excel 处理与数据分析能力**；想增强某方面专业能力，就修改/添加对应 Skill；
- 关键代码只有 `create_deep_agents(..., skills=[...])`——DeepAgents 框架负责其余 harness 部件；运行中框架会在需要时加载 Skill 并按其指令完成任务；
- 同类支持 Skill 的产品还有 Qoder、WorkBuddy、OpenClaw 等。值得一提的对照：OpenClaw 这类产品会**在你本机上直接执行** Python/TypeScript 等生成的代码——课堂提到"什么代码都会运行"正是这类产品此前被诟病的安全点；回看实验 10 的沙箱设计，更能理解"隔离执行"为什么重要。

---

## 专题 A：AI 辅助编码的纪律

**现场反例**：讲师在查 Text2SQL 的数据库位置时，顺口让编码助手直接做了代码修改——事后自述"特别后悔"：应该先用 **ask 模式**问（只答不改），而不是在 agent 模式下让它直接动手；最后用 undo 回退了这次改动。

**正确姿势**（课堂总结）：

1. 先用 **ask 模式**问清楚；
2. 需要改代码时，用 **plan 模式**（或 openspec 一类的 spec 流程）：修改前先写清"为什么要改、计划怎么改"，按 plan 一步步执行；
3. **plan 文件与代码改动一起 commit 提交**，留下可追溯的决策记录。

## 专题 B：Skill 沉淀方法论

课堂以"PDF → 知识库 → Skill"的完整开发过程收尾（前一天课堂构建的知识库即本仓库 `wiki_kb_agent_demo/knowledge_base/` 下的工行年报材料）：

**开发流程**

1. 对话式下达目标："对 PDF 文件进行分析（说明分析方式），构建逐级目录，构建 md 索引，**形成知识库**"——最后半句很重要，让模型理解写 index 的目的，产出结构才合理；
2. 模型遇到不清楚的会反问，回答后继续；复杂任务可先让它写 spec，人审 spec、提修改意见（比如"不要把附件放进去"）后再执行；
3. 交付后人工审核——**审核是沉淀 Skill 的起点**。

**讲师的开发习惯（方法论核心）**

- 先让 AI 干活：一把干对就不管它；
- 干错了或产出不符合预期，就明确指出哪些地方不一样；
- 然后让 AI **自己**把这些差异沉淀成 Skill——"就像让下属'你再想想'，不替他写，因为现在它可能比我知道得多"。

**案例：图片处理**

初版知识库构建没处理 PDF 中的图片。被追问后模型自己提了方案，但讲师没采用，而是按自己的思路要求：不同类型的图拆开、用不同方式处理。例如"获奖类"图片不做泛泛描述，而是**结构化表达**：什么奖、第几名、谁颁发的。修正后的做法被封装进 Skill（主 SKILL.md 讲流程，附属子 md 讲图表处理规范），下次同类文档进来就会按规范处理。

**持续改进**：Skill 里的例子要贴近自己的业务——示例是"股东会/公司组织结构"图，如果你处理的是铁路信号流程图，就换成流程图的例子，便于模型更好理解后续任务。

**收尾范式**：知识库与 Skill 在对话式环境里开发 → 在 coding agent 里验证测试 → 导入自己用代码写的 agent 环境直接运行。"代码构建智能应用 + 对话式开发知识库/Skill"两条线在此汇合。

---

## 附录一：转写纠错对照表

录音转写中的高频识别错误（正文已全部使用正确写法）：

| 转写原文 | 正确写法 | 备注 |
|---|---|---|
| 方根 call / 方生 call / 方审 / 方根 | Function Call(ing) | |
| 扣子 define | Coze / Dify | 低代码工作流平台 |
| circle / circle line | SQL / SQLite | |
| Streamlet | Streamlit | |
| mcb | MCP | |
| pandantic / pandas（指结构化库处） | Pydantic | |
| R one / RY / 阿万 | DeepSeek-R1 | "V"指 DeepSeek-V3 |
| future 的提示 | few-shot 提示 | |
| GRPU | GRPO | DeepSeek 提出的强化学习训练范式 |
| cloud code / clawcode | Claude Code | 后者视为口误重复 |
| agent 的 honey / hannes | Agent Harness | |
| Deep Agents | DeepAgents | 框架名 |
| 千万 B / 千万 3,6,27 | qwen3.6-27b 等 Qwen 中档模型 | 与代码默认模型名一致 |
| chatall | ChatALL | GitHub 项目 ai-shifu/ChatALL |
| walker body / work body | WorkBuddy | 腾讯 AI Agent 办公工具 |
| cute | Qoder | 讲师课堂使用的 IDE |
| inbeding | embedding | |

**特别提示**：Qwen3.8-Max、Hermes Agent、OpenClaw、WorkBuddy、Qoder 经核实均为**真实存在**的产品名，不属于转写错误，正文直接使用。

## 附录二：待评估回填实验手册的候选清单

以下要点来自课堂、目前手册正文尚未覆盖，供复盘后决定是否回填（精简版）：

| 实验 | 候选回填要点 |
|---|---|
| 0 | OpenAI 兼容 API 已成事实标准（厂商 + 智能体产品如 Hermes Agent 均支持）；可让 AI 助手代配环境 |
| 1 | 通信"网格"系统提示词案例；temperature 一句话说明 |
| 2 | 多轮对话本质 = 不断 append messages（原理层面点透） |
| 3 | 结构化输出的两大动机：字段级对接下游系统、省 token |
| 4 | function calling 第一性原理表述与 ERP 类比；DeepSeek-R1 few-shot 案例（本质是提示词） |
| 5 | "公式/规则/小模型封装成工具，不让 LLM 硬算"的教学主张 |
| 6 | 20×20=400 的动机算术；"MCP 把对接代码挪到 server 只写一遍"的准确表述 |
| 7 | "跟 AI 说话要说清楚"的 ECS 拒答案例；RAG 即幻觉抑制手段 |
| 9 | EDA 玩法：让 AI 对数据库做探索性数据分析 |
| 10 | Excel 场景动机链；"AI 生成的代码必须在沙箱运行"的明确表述 |
| 11 | Agent 演进总结线；OpenClaw 本地执行 vs 沙箱的安全对照 |
| 新章节 | 专题 A（AI 辅助编码纪律）与专题 B（Skill 沉淀方法论）可考虑作为手册第 12 节或附录 |

**另发现一处文档与代码不一致**（与本次转写无关，复盘时可一并处理）：
实验手册 0.3 称"根目录的脚本**没有**内置 `load_dotenv()`"，但实际核查全部根目录脚本（`first_llm_call.py`、`first_llm_app.py`、`structured_output_demo.py`、`mini_rag_demo.py`、`text2sql_demo.py` 等）均已在启动时显式 `load_dotenv()` 加载根目录 `.env`。手册该处表述需要修正（`set -a` 做法仍然兼容且无害，但对已内置 `load_dotenv` 的脚本并非必需）。
