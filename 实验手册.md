# LLM 应用开发实验手册

> 面向学员的一步步引导式实验。每个实验都经过实机验证，命令可直接复制执行。
> 全文约需 3~4 小时（含阅读），每个实验独立，可按需选做。

**AI 辅助学习建议**（来自课堂实践）：

- 命令不必死记：把本手册交给 IDE 里的编码助手参考，让它帮你准备环境、执行命令；
- 分清助手模式：ask 只答不改文件，agent 会实际修改，plan 先写修改计划再执行——让 AI 改代码前先 ask 问清楚，修改走 plan，并把 plan 与代码一起提交留痕；
- 看不懂的代码随时让 AI 讲解，每个 demo 都是自包含脚本。

> 延伸阅读：[课堂实录与教学要点.md](课堂实录与教学要点.md)——课堂讲解要点、真实案例与现场实录（含转写纠错对照表）。

## 学习路线总览

```
基础调用 ──→ 结构化输出 ──→ Function Calling ──→ LangGraph 图编排
                                                                     │
Excel 分析应用 ←── Text2SQL ←── Wiki导航Agent ←── RAG ←── MCP ←── Agent Harness ←┘
```

| # | 实验 | 核心知识点 | 预计耗时 |
|---|---|---|---|
| 0 | 环境准备 | venv、环境变量、set -a | 15 分钟 |
| 1 | 单次调用 | client / messages / completions | 10 分钟 |
| 2 | 流式多轮对话 | stream、对话历史、推理模型 | 20 分钟 |
| 3 | 结构化输出 | json_schema、Pydantic、降级链路 | 20 分钟 |
| 4 | Function Calling | tools 参数、工具循环 | 25 分钟 |
| 5 | LangGraph | 状态图、条件边、预构建 agent | 25 分钟 |
| 6 | MCP 协议 | FastMCP、Streamable HTTP、工具发现 | 30 分钟 |
| 7 | RAG | 切块、embedding、向量检索、生成 | 30 分钟 |
| 8 | Wiki 导航式知识库 Agent | 零 embedding、工具导航、引用溯源 | 20 分钟 |
| 9 | Text2SQL | schema 注入、SQL 执行与自修复 | 20 分钟 |
| 10 | Excel 分析应用 | Streamlit、代码沙箱、全栈集成 | 30 分钟 |
| 11 | Agent Harness + Skills | 渐进式披露、Skill 开放标准、harness 部件 | 30 分钟 |

---

## 实验 0：环境准备

### 0.1 创建虚拟环境并安装依赖

```bash
cd <你的仓库目录>
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip          # ⚠️ 重要：旧 pip 可能无法识别新包的预编译 wheel
pip install -r requirements.txt
```

**踩坑实录**：旧版 pip（24.0）安装 `cryptography` 时找不到预编译 wheel，退回源码编译并要求 Rust 工具链而失败。**先升级 pip** 可避免。

### 0.2 配置 API Key

```bash
cp .env.example .env
```

编辑 `.env` 填入你的 Key 与端点：

```bash
API_KEY=<你的Key>
API_BASE=<OpenAI兼容端点，如 https://api.siliconflow.cn/v1/>
MODEL=<默认对话模型>
```

OpenAI 兼容 API 已是事实标准：主流模型厂商与智能体产品（如 Hermes Agent）普遍提供 OpenAI 兼容端点，本教程的调用方式学一次处处可用。

### 0.3 环境变量：脚本自动加载，终端引用用 `set -a`

各脚本启动时**已内置** `load_dotenv()`，会自动加载根目录 `.env`，跑脚本一般无需手动导出。但当你需要在**终端里直接引用变量**（如 0.4 的 curl 探测）时，每个新终端先执行：

```bash
set -a; source .env; set +a
```

- `set -a`：开启自动导出，之后的变量赋值都变成环境变量（子进程可见）；
- `source .env`：执行 .env 里的 `KEY=value` 赋值；
- `set +a`：关闭自动导出。

对已内置 `load_dotenv()` 的脚本，多做这一步无害，还能用 `echo $MODEL` 确认配置生效。

### 0.4 探测你的 Key 能用哪些模型

很多平台（尤其企业专属实例）对 Key 有模型白名单。**开跑前先探测**：

```bash
curl -s -H "Authorization: Bearer $API_KEY" "$API_BASE/models" \
  | python3 -c "import json,sys; [print(m['id']) for m in json.load(sys.stdin)['data']]" | head -20
```

再对候选模型发最小请求验证：

```bash
python3 -c "
import os
from openai import OpenAI
c = OpenAI(api_key=os.getenv('API_KEY'), base_url=os.getenv('API_BASE'))
print(c.chat.completions.create(model='<模型名>', messages=[{'role':'user','content':'你好'}]).choices[0].message.content)
"
```

**错误对照表**（本教程实测总结）：

| 现象 | 含义 | 处理 |
|---|---|---|
| 403 Access denied by API-Key restrictions | Key 的模型白名单不含该模型 | 换白名单内模型 / 找管理员放通 |
| 401 Api key is invalid | Key 被发到了不认它的端点 | 检查 `API_BASE` 是否真的生效了 |
| 404 model not found | 模型名写错 | 用 `/models` 列表核对 |

---

## 实验 1：最简单的单次调用

```bash
python first_llm_call.py
# 或指定模型：python first_llm_call.py --model <你的模型>
```

**观察点**：
- `messages` 按 `system/user/assistant` 组织——LLM 无状态，"记忆"全靠把历史传回去；
- 脚本预置了多轮历史，模型回答会基于"我是张三"的上下文。

**系统提示词的价值（课堂案例）**：
- 本脚本的 system 提示词是"你在回答博士生的提问。"——同样的提问，去掉这句后回答会明显更简略，提示词确实引导了回答的深度与风格；
- 行业案例：某通信公司的"网格"指地下管线剖面网格，AI 起初总误解为"网格计算"；把业务语境写进系统提示词后立刻理解。客户说"大模型不懂我们行业"时，先试试补系统提示词。

**常见报错**：若报 403/401，对照实验 0.4 的错误对照表排查。

---

## 实验 2：流式多轮对话与推理模型

```bash
python first_llm_app.py
```

**体验清单**：
1. 问一个需要推理的问题（如"房间里有3个人，走了2个，来了5个，现在有几个人？"）；
2. 观察**灰色思考过程**（`reasoning_content`）与**终端默认色正式回复**（`content`）的分段显示；
3. 追问依赖上文的问题，验证多轮记忆；
4. 直接回车退出。

**踩坑实录（已修复，值得理解）**：推理结束时服务端会发"过渡 chunk"——`reasoning_content=""`（空串而非 None）且 `content` 带答案首 token。若代码用 `is not None` 判断 + `if/elif` 互斥分支，答案 token 会被丢弃，表现为"只有思考没有回答"。正确写法：**真值判断 + 两个独立 if**。

> 若你的模型没有思考能力，只会看到默认色回复，属正常。
>
> 全仓库 demo 统一着色约定：**灰色 = 过程信息**（思考/工具调用/中间步骤/日志），**终端默认色 = 最终回答与结果**。

---

## 实验 3：结构化输出

```bash
python structured_output_demo.py
```

**知识点**：
- 为什么需要：成段文字没法直接写数据库，结构化输出让每个字段可直接对接下游系统；且只输出必要字段，节省 token；
- 首选 `response_format={"type": "json_schema", "strict": true}`——服务端保证输出符合 Schema；
- Schema 直接由 Pydantic 模型生成（`model_json_schema()`），**模型定义即契约**；
- **三级降级链路**（本脚本已内置，很多部署会走到第 2 级）：
  1. `json_schema` 严格模式 → 400 不支持时
  2. `json_object` 宽松模式 + **Schema 写进提示词**（宽松模式服务端不传 Schema 给模型，不写进 prompt 会丢字段）→ 最后
  3. Pydantic `model_validate_json` 校验兜底
- **结构化输出同样支持流式**（本脚本已开启）：`stream=True` 与 `json_schema`/`json_object` 可同时使用，`delta.content` 流出的就是 JSON 文本片段。注意中途片段大多不是合法 JSON，不能跳过末尾的 Pydantic 完整校验

**踩坑实录**：思考模型开着思考模式做 JSON 输出会失控（实测产出 9 万字符垃圾文本）。对思考模型做结构化输出要 `extra_body={"enable_thinking": False}` + `max_tokens` 上限。

**观察点**：李华的 `phone` 应为 `null`（文本说"电话回头再给"），验证模型理解 Optional 语义。

---

## 实验 4：Function Calling——Agent 的最小内核

```bash
pip install PyGithub
cd function_call_and_agent_demo
python demo-agent-with-tools-arg.py
```

**观察点**（对着日志数循环）：
1. 第 1 轮：模型不回答，返回 `tool_calls: get_repo_tree`（先侦察目录）；
2. 中间轮：根据目录挑文件，`get_repo_file_content` 读内容，可能多轮；
3. 收尾：某轮无 tool_calls → `Final Answer` 逐字流式打印。

**核心机制**：`tools` 参数描述函数 → 模型返回调用请求（函数名+JSON 参数）→ 你执行 → `role="tool"` 消息回填 → 循环。这是**所有 Agent 的内核**。第一性原理：模型只能输入/输出 token，自己调不了外部系统——所谓调用，是模型输出一段"调用表达"，由另一段代码代为执行并把结果回填。

**流式技巧**（本脚本已实现，看 `call_with_messages`）：`stream=True` 下 content 与 tool_calls 都是增量片段——content 逐字打印即可，tool_calls 要按 `index` 归位：首个片段带 id 与函数名，后续只是 arguments 文本碎片，逐个拼回完整调用。

**思考题**：如果工具是"删数据库"，这个循环缺什么？（答案见实验 5 末尾）

> 无 `GITHUB_TOKEN` 时会匿名访问公开仓库（60 次/小时限额），教学够用。

**选做延伸**：官方未支持工具调用的模型（如当年的 DeepSeek-R1）也能做智能体——云服务商在接口层注入 few-shot 提示示范标准输出格式即可。function calling 本质是提示词工程（详见[课堂实录](课堂实录与教学要点.md)实验 4）。

---

## 实验 5：LangGraph——把手写循环变成"图"

```bash
cd <仓库根目录>
python function_call_and_agent_demo/langgraph_demo/langgraph_agent_scaffold.py
```

**观察点**：
- 写法一（手搓 StateGraph）：`pretty_print()` 完整轨迹 `Human → AI(tool_calls) → Tool → AI(tool_calls) → Tool → AI(最终答)`；
- 写法二（`create_react_agent`）：一行代码等价实现；
- 默认问题会触发**两次**工具调用，观察多轮循环。

**教学主张**：公式计算、行业小模型、数据查询、规则判断应封装成工具给模型调，不提倡让模型靠深度思考硬算——准确率、速度、token 三重代价都吃亏，而函数是毫秒级且结果确定。本实验的 `celsius_to_fahrenheit` 就是示例：比较判断归模型，数值换算归工具。

**实验 4 思考题答案**：缺执行前的控制点（审批/权限）。LangGraph 的解法——在 `agent → tools` 边上加审批：

```python
# 方式A（零代码）：
graph = workflow.compile(checkpointer=MemorySaver(), interrupt_before=["tools"])

# 方式B（细粒度）：自定义审批节点 + interrupt()，可按工具名决定放行/拦截
```

`interrupt()` 把执行状态序列化进 checkpointer，进程退出也能断点恢复——手写循环与低代码平台（Coze/Dify 的黑盒 Agent 节点）都给不了这个能力。

---

## 实验 6：MCP 协议——工具即服务

需要**两个终端**。

**终端 1——启动 MCP 服务器**：

```bash
python function_call_and_agent_demo/mcp_server_demo.py
# 看到 Uvicorn running on http://127.0.0.1:8000 即就绪
```

**终端 2——运行客户端 agent**（脚本会自动加载根目录 `.env`；如需在终端直接引用变量，可选执行 `set -a; source .env; set +a`）：

```bash
python function_call_and_agent_demo/mcp_client_agent_demo.py
```

**为什么需要 MCP**：若每个智能体各自实现外部系统对接，20 个智能体 × 20 个系统 = 400 段散落代码，维护是灾难。MCP 不是省掉对接代码，而是把它挪到 server 只写一遍，所有 client 共享，并随外部系统接口同步演进。

**观察点**：
1. `已加载 3 个 MCP 工具`——工具清单**运行时从服务器发现**，客户端代码里没有它们；
2. 工具执行发生在服务器进程，客户端只拿结果；
3. 模型对远程工具与本地工具的使用方式完全一致。

**选做**：杀掉服务器再跑客户端，观察失败发生在"连接/工具发现"阶段——体会"工具即服务"的边界。

**版本注意**：`mcp` SDK 2.0 移除了 `fastmcp` 高层封装，本仓库钉在 1.x（`mcp[cli]>=1.9,<2`）。

---

## 实验 7：RAG——检索增强生成

```bash
python mini_rag_demo.py                                  # 交互模式
python mini_rag_demo.py --query "ECS无法远程连接怎么排查" --top_k 3
```

**管线全貌**：

```
335篇文档 → HTML清洗 → 按句切块(800字/100重叠, 共996块)
→ embedding 批量向量化(缓存到 rag_index.npz) → 余弦检索 Top-K
→ 上下文拼装 → LLM 依资料流式生成（带拒答约束）
```

**观察点**：
1. 首次运行建索引（约 1 分钟），之后秒级加载缓存；
2. `[检索结果]` 的相关度分数（实测目标文档 0.87 命中）；
3. 问一个语料里没有的问题（如"怎么做红烧肉"），验证模型**拒答**而非编造——这是 RAG 的灵魂；
4. 提问要具体：实测只敲"ECS"会因检索不中而拒答，改问"ECS 无法连接怎么查"才命中——RAG 不瞎说，是天然的幻觉抑制手段。

**思考题**：检索只返回相似块，如果答案散落在多个块里怎么办？（提示：rerank 重排、增大 top_k、查询改写）

---

## 实验 8：Wiki 导航式知识库 Agent——另一种 RAG 范式

```bash
cd wiki_kb_agent_demo
python kb_agent.py                                   # 交互模式
python kb_agent.py --once "什么是分块策略？"          # 单轮提问
```

**与实验 7 的对照——同一问题，两种范式**：

| | 实验 7（embedding 检索） | 本实验（导航式） |
|---|---|---|
| 预处理 | 切块 → 向量化 → 存索引 | **零**，copy 进来即用、永远最新 |
| 检索方式 | 向量余弦相似度 | 模型从 `index.md` 读目录，顺链接迭代深入 |
| 文档结构 | 切块后丢失 | 目录 + 链接被完整利用 |
| 可解释性 | 相似度分数（黑盒） | `🔧 调用工具...` 轨迹全程可见 |

> 这就是 PageIndex 的思想：用“目录树 + LLM 推理导航”替代向量相似度，适合中小规模、结构良好的 wiki；海量文档才需要向量检索。

**观察点**：
1. 每次提问先看 `🔧 调用工具` 行——模型的第一次调用必是 `read_page('index.md')`（系统提示词约定的导航流程）；最终回答在 `助手:` 后逐字流式打印；
2. 答案末尾带引用，如 `（来源：02-rag.md#分块策略）`——可点开文件核对；
3. 按顺序试这四问，覆盖不同导航路径：
   - `RAG 常见的坑有哪些？` → 单页直达；
   - `Agent 最小实现是什么？它和 RAG 有什么关系？` → **跨页面**：看它是否顺页面内链接串联多个文件；
   - `rerank 是什么？` → **兜底路径**：目录里没有独立页面，看它会不会改用 `grep_kb` 全文定位；
   - `知识库里有关于数据库的内容吗？` → **诚实性**：库里没有，看它是否如实说没有而不编造。

**知识点**：
- 三个工具分工：`read_page` 主力导航，`list_pages`/`grep_kb` 目录失灵时兜底；
- wiki 的 `index.md` + 页面互链本身就是高质量索引——知识库的组织结构即检索结构；
- `MAX_TOOL_ROUNDS` 限制探索轮数，防止无限循环。

**选做**：把自己的 wiki 整体 copy 进 `knowledge_base/`（先删示例的 4 个 `.md`），重跑即可，无需任何预处理；入口页不叫 `index.md` 时改 `kb_agent.py` 里的 `ENTRY_PAGE` 常量。

**思考题**：知识库规模大到导航变慢时该怎么办？（提示：把向量检索作为**补充工具**加回工具列表，导航与检索并存，由模型自己选用。）

---

## 实验 9：Text2SQL——自然语言查数据库

```bash
python text2sql_demo.py
python text2sql_demo.py --query "卖得最好的5首歌曲是什么？属于哪些专辑？"
```

**知识点**：
- `PRAGMA table_info` 自动内省 schema 注入提示词——模型"看得见"表结构，**无需任何训练**；
- `execute_sql` 工具：read-only 连接 + 仅允许 SELECT/WITH（双层安全）；
- SQL 报错回传模型 → **自修复重试**。

**观察点**：默认复合问题会被拆成多轮查询（国家总消费 → Top3 客户），数一数轮次；`[最终回答]` 为逐字流式打印。

**选做**：让模型对这个音乐商店库做探索性数据分析（EDA）——它会自行脑爆分析视角（最大的播放列表、收入高低点、热门艺术家、客户分布等）并逐条写 SQL 验证后汇总发现。

---

## 实验 10：Excel 数据分析应用——全栈集成

前置：安装并启动 **Docker**。

**为什么做这个实验**：更复杂的分析光靠 SQL 不够，要写 Python 执行；真实业务数据大量在 Excel 里（几乎所有业务系统都支持导出 Excel），跨系统拉通分析普遍依赖 Excel——"AI 处理 Excel"是典型且长期存在的场景。

```bash
cd function_call_and_agent_demo/excel-process
pip install -r requirements.txt
streamlit run app.py
```

浏览器打开后：上传仓库里的 `2023年8月-9月销售记录.xlsx` → 输入"统计各品类销售额并画柱状图" → 观察 agent 生成代码、沙箱执行、出图。

**安全原则**：AI 生成的代码不是直接在本地跑，而是在沙箱（隔离容器）里运行——这正是本应用依赖 Docker 的原因。

**本应用实测踩过的坑（都已修复，代码里有注释）**：
1. llm-sandbox 每个会话是独立容器，会话关闭即销毁——跨会话共享文件必须 `keep_template + commit_container`；
2. `copy_to_runtime` 必须传**完整文件路径**，传目录会把文件解压到错误位置；
3. LLM 偶尔漏传"必填"工具参数（如 `lang`）——工具执行端要做防御性默认；
4. LLM 容易拼错长绝对路径——让工具支持相对路径、提示词引导用短文件名。

**无头验证**（不经 UI 直接测 agent 链路）：

```bash
python test_e2e_headless.py
```

---

## 实验 11：Agent Harness 与 Skills——框架化的完整外壳

前置：安装 **LibreOffice**（公式重算依赖）；装依赖：

```bash
cd function_call_and_agent_demo/agent_harness_demo
pip install -r requirements.txt
```

```bash
python agent_harness_demo.py --model <你的模型> --base_url <你的端点>
```

默认任务：分析 `data/` 里的销售 Excel，按 xlsx skill 规范产出 `report.md` + 带公式的 `output/report.xlsx`，并跑公式重算校验。

**观察点**（本实验的主角是**Skill 三层渐进式披露**，对照轨迹看）：

| 层级 | 轨迹表现 | 原理 |
|---|---|---|
| 1. 元数据 | 不可见（启动即注入系统提示词） | 提示词里只有 xlsx 的一句话描述，agent 凭它判断相关性 |
| 2. 指令 | `read_file('skills/xlsx/SKILL.md')`，带 `<<< 第 2 层` 标记 | 判定任务匹配后才读取完整 9KB 指令 |
| 3. 资源执行 | subprocess 调 `scripts/recalc.py`，返回 `"status": "success"` | 附属脚本不只被读，还被真正执行 |

**产物自检**：打开 `output/report.xlsx`——多个工作表、全部是 `SUMIF`/`SUMPRODUCT` 真实公式而非硬编码数值（正是 SKILL.md 推荐的"Excel-2007 时代函数"）。Skill 是**自然语言指令包**，规范生效的直接证据就在产物里。

**错误自愈**：中途大概率出现代码报错（`NameError`、路径拼错），看 agent 如何从异常栈里自行修正重试——这是 harness 把工具错误回传模型的循环设计。

**负例对照**（强烈建议跑）：

```bash
python agent_harness_demo.py --model <模型> --base_url <端点> \
    --task "总结 data/ 目录里有什么数据，给出一段文字描述"
```

预期轨迹中**不出现**读 SKILL.md——9KB 指令始终不进上下文。一句话元数据的成本，换来"知识只在相关时占用上下文"，这就是渐进式披露的价值。

**知识点**：
- Agent Skills 是开放标准（SKILL.md），同一份 skill 可跨 harness 使用（Claude Code、Cursor、DeepAgents 等 27+ 平台）；但**格式通用 ≠ 行为通用**，各家的扫描路径、加载触发、权限控制各自为政；
- harness 内置部件：`write_todos` 规划、虚拟文件系统、`task` 子代理派发、上下文超长自动摘要——手写循环要自己实现的东西，这里都有现成的。

**常见报错**：403 即 Key 的模型白名单问题，对照实验 0.4；管道（`| tee`）下看不到实时输出属缓冲现象，直接看 `output/` 目录即可；Windows 用户见 demo 目录内 `实验指南.md` 的"Windows 用户完整操作"（代码已做 LibreOffice 路径与编码适配）。

**思考题**：如果把 SKILL.md 全文直接塞进系统提示词，会付出什么代价？（提示：token 成本、多技能共存时的上下文竞争、无关指令对模型的干扰。）

**演进总结线**：基础调用 → 多轮对话 → 结构化输出 → function calling → 框架/MCP/数据分析 → Skill。至此，模型不再是框在黑盒里的模型，而是能感知外部数据、推动外部系统的智能体。

---

## 进阶自阅

- `finance_doc_review/`：生产风格完整项目——LangGraph 编排 + YAML 规则引擎 + Pydantic AI 结构化输出，见其 README；
- `function_call_and_agent_demo/archive/`：归档的旧写法（手写 ReAct prompt 解析等），每个文件注明了归档原因与替代方案，是理解"为什么现代写法长这样"的好材料。

## 通用排错口诀

1. **401 查端点，403 查模型**（Key 和 base_url 是绑定的，白名单是常见的 403 原因）；
2. 脚本已自动加载 `.env`；仅在终端直接引用变量时先 `set -a; source .env; set +a`，用 `echo $MODEL` 确认生效；
3. 思考模型做结构化输出先关思考；
4. 沙箱/容器的文件默认"阅后即焚"，跨会话要显式持久化。
